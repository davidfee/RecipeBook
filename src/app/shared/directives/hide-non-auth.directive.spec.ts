import { HideNonAuthDirective } from './hide-non-auth.directive';

describe('HideNonAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new HideNonAuthDirective();
    expect(directive).toBeTruthy();
  });
});
